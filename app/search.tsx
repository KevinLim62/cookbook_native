import { View, Text, SafeAreaView } from 'react-native'

type Props = {}

const search = (props: Props) => {
  return (
    <SafeAreaView>
      <Text>
        Search your recipes
      </Text>
    </SafeAreaView>
  )
}

export default search