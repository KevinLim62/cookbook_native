import { View, Text, SafeAreaView } from 'react-native'

type Props = {}

const profile = (props: Props) => {
  return (
    <SafeAreaView>
      <Text>
        Your profile
      </Text>
    </SafeAreaView>
  )
}

export default profile